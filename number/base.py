'''
python3的整型没有大小限制，可以当Long使用，浮点数也没有，不过超出一定范围会直接表示为inf（无限大)
整数除法总是返回浮点数，用//向下取整
算术，+ - * / % **(幂) //(取整除，向下取整)
+= -= *= /= %= **= //=
位，& | ^ ~ << >>
优先级，**最高

注意
    //不一定就是整数，要根据除数和被除数的类型，若是浮点数则结果也是浮点数
转number
    int(x), float(x)
'''
